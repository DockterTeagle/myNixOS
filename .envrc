# export TMUX_SESSION_NAME="myNixOS"
watch_file ./flakeModules/devenv.nix
watch_file ./flakeModules/treefmt.nix
use flake . --impure
# if ! tmux has-session -t "$TMUX_SESSION_NAME" 2>/dev/null; then 
#   tmuxinator start myNixOS
# fi
