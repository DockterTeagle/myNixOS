---@module "lazy"
---@type LazySpec
return {
  -- {
  --   "folke/lazydev.nvim",
  --   ft = "lua", -- only load on lua files
  --   opts = {
  --     enabled = function(root_dir)
  --       return not vim.uv.fs_stat(root_dir .. "/.luarc.json")
  --     end,
  --   },
}
